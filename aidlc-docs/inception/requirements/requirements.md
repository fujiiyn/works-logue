# Works Logue — 要件定義書

## ドメインモデル（概念定義）

### Planter（プランター）
Seed・Log・Louge を内包する**成長の単位**。UI上には直接「Planter」という言葉は出ないが、システムの中核概念。

ユーザーは常に**同一の Planter ページ**にアクセスし、その状態に応じた表示を見る。開花しても別ページへ遷移するのではなく、同じ Planter の状態が変化する。

**Planter の状態遷移**：

```
seed ──→ sprout ──→ louge
 (種)     (成長中)    (開花)
```

| Planter 状態 | UI バッジ | 条件 | 内容 |
|---|---|---|---|
| `seed` | **Seed** | Log が 0 件 | 初期状態 |
| `sprout` | **Sprout** | Log が 1 件以上 | 条件A/B が進行中（内部で Sprout 1〜3 を区別） |
| `louge` | **Louge** | 条件A AND 条件B 完全充足 | AI 記事（Louge）が生成された状態 |

**Sprout サブステート（内部区別）**:

| サブステート | 条件 | Progress Bar | 意味 |
|---|---|---|---|
| Sprout 1 | Log 1件〜、条件A進行中 | 0〜50% | 芽が出始め |
| Sprout 2 | 条件A充足、条件B進行中 | 50〜80% | 成長中 |
| Sprout 3（蕾） | 条件B が bud_threshold（初期値80%）に到達 | 80〜99% | もう少しで開花 |

### Seed / Log / Louge の関係
- **Seed**：Planter の起点となる投稿
- **Log**：Seed に対して蓄積される知恵・コメント。Log が増えるほど Louge スコアが上昇し Planter が成長する
- **Louge**：flowering 状態に達した Planter が持つ AI 生成ナレッジ記事。Seed・Log とともに同一 Planter ページ上に表示される

---

## Intent Analysis

| 項目 | 内容 |
|---|---|
| User Request | Works Logueの開発を始めたい |
| Request Type | New Project（グリーンフィールド） |
| Scope Estimate | System-wide（新規プラットフォーム全体） |
| Complexity Estimate | Complex（AI統合・スコアリング・リアルタイム集合知）|

---

## MVP スコープ（標準）

以下の機能を初期リリースに含める：

- Seed 投稿（疑問・悩み・失敗など8タイプ、単一画面フォーム）
- Log 投稿（Seedへのコメント・知恵の蓄積）
- Louge スコアリング（Log 投稿のたびに自動計算）
- Sprout ステータス表示（スコア閾値による段階1〜3 + 開花）
- Louge 開花（AI による自動記事生成）
- ユーザープロフィール（自己紹介・タグ・アバター・投稿履歴）
- インサイトスコア（Louge 開花への貢献度スコア）

バッジシステム・Fork 機能は **フェーズ2** 以降に実装する。

### 管理者機能（MVP）
- ユーザー制限（BAN / 解除）
- Planter アーカイブ・削除
- マスタデータ管理（Seed ジャンル・タグ等）

---

## 機能要件

### FR-01: 認証・ユーザー管理
- メール＋パスワード認証（Supabase Auth）
- Google OAuth ログイン（Supabase Auth）
- プロフィール編集

### FR-02: ユーザープロフィール
- 表示名・自己紹介文（テキスト）
- アバター画像（Supabase Storage にアップロード）
- タグ設定：業界タグ・職種タグ・スキルタグ（`docs/tags.md` 準拠、複数選択）
- 投稿した Seed 一覧・Log 一覧・参加した Louge 一覧
- インサイトスコア表示

### FR-03: Seed 投稿
- **単一画面フォーム**（ステップ型ではない）で以下を入力：
  - 投稿タイプ選択（`seed_types` マスタから取得。初期値: 疑問/悩み/失敗/仮説/比較/違和感/シェア/実践報告。各タイプに説明文を持つ）
  - タイトル
  - 本文
  - タグ（業界・職種・スキル、`docs/tags.md` 準拠）
- 非ログインユーザーは閲覧のみ・投稿はログイン必須

### FR-04: Log 投稿
- Seed に紐づくスレッド形式のコメント
- テキスト投稿・返信（ネスト1段）
- 投稿者プロフィールへのリンク
- **Log 投稿のたびに Louge スコアを再計算する**（FR-05 参照）

### FR-05: Louge スコアリング（2段階ロック解除方式）

Louge（開花）の生成には **条件A（構造判定）** と **条件B（成熟度スコア）** の **両方（AND）** を満たす必要がある。

---

#### 条件A: 構造の成立（パターンランゲージ的視点）

AI が Log 全体を読み取り、ビジネスノウハウとしての「必須パーツ（型）」が埋まっているかを判定する。愚痴や共感だけで「解決策」が提示されていない状態では開花しない。

| パーツ | 内容 | 例 |
|---|---|---|
| 状況（Context） | どのような前提条件・環境で起きる事象か | 新規事業立ち上げ時、予算ゼロ |
| 問題（Problem） | そこで発生するジレンマや障害は何か | 大手の看板が通じず、営業責任者の心が折れる |
| 解決策（Solution） | 具体的にどういう行動・仕組みで突破したか | スタートアップ経験者を優先、お試し業務委託期間の導入 |
| パターン名（Name） | AI がこのノウハウに「名前」を付けられる状態か | 「0→1営業責任者の泥臭さ要件」 |

**実行タイミング**: Log 投稿ごとに軽量チェック。条件A の充足率（構造パーツがどれだけ埋まったか）を算出し、UI 上の Progress Bar に反映する。

---

#### 条件B: 成熟度スコア（4観点の AI 評価）

条件A の「型」が埋まった上で、その中身が「集団知性」と呼ぶにふさわしい質と熱量に達しているかを AI が4観点でスコアリングする。

| 観点 | 判定基準 | NG 例 |
|---|---|---|
| 網羅度（Comprehensiveness） | 原因・対策・予防策など、テーマ全体を俯瞰できているか | 表面的な結果論しかない |
| 多様性（Diversity） | 異なる背景を持つ複数ユーザーが Log を投下しているか | 特定の2〜3人だけで完結 |
| 反論/例外（Counterarguments） | 「このケースでは逆効果」等の検証ログが含まれるか（**特に重要**） | 全員が同調しているだけ |
| 具体性（Specificity） | 明日から実行できるレベルのアクションが抽出可能か | 「意識を変えるべき」等の抽象論 |

**実行タイミング**: 条件A 充足 かつ 最低参加ライン（例: 5人以上参加、10 Log 以上）を超えた時点で、AI が条件B の本格スコアリングを実行する。

---

#### スコア不足時の AI ファシリテーション

条件B の総合スコアが基準値に達しない場合、AI は Louge を生成せず、代わりに **AI からの問いかけ（ファシリテート）** を Log として投稿し、議論を誘導する。

- 反論/例外が不足時: 「素晴らしい解決策が集まっていますが、逆にこの手法を試して『失敗した』という経験をお持ちの方はいませんか？」
- 具体性が不足時: 「〇〇という意見に共感が集まっていますが、実際に現場で使う『フォーマット』や『ツール』の具体例はありますか？」

---

#### Planter 状態遷移

| Planter 状態 | 条件 | UI バッジ |
|---|---|---|
| `seed` | Log が 0 件 | Seed |
| `sprout` | Log が 1 件以上（条件A 充足率が上昇中） | Sprout |
| `louge` | 条件A 充足 AND 条件B スコア基準突破 | Louge |

- **Sprout 内のサブステート**（Sprout 1〜3）は上記条件で区別（UI はビジュアルの細かい変化で表現）
- UI バッジは `seed` / `sprout` / `louge` の3種のみ
- **Progress Bar は開花までの総合進捗を表示**（0%〜100%、100% = 開花）
  - 0〜50%: 条件A の充足率をスケール（構造パーツが揃うほど伸びる）
  - 50〜100%: 条件B の成熟度スコアをスケール（条件A 充足後に伸び始める）
  - Sprout 3（蕾）= バーが 80% に到達した状態

**設計方針**:
- 条件A・条件B の判定ロジックはポリシークラス/設定ファイルとして実装し、閾値・パーツ定義を変更しやすくする
- 各判定結果のログを DB に保存し、改良時の分析・比較を可能にする
- AI ファシリテート Log は `is_ai_generated` フラグで通常の Log と区別する

### FR-05b: UI レイアウト構造

**参照 Figma**: `RKsHkKG2GfOlaVxRzY81TG` node `12:3`（Home）

**全画面共通レイアウト（3カラム）**：

```
+--Header-------------------------------------------------------+
| [Logo] [Works Logue]         [Bell] [ログイン] [+ Seed]        |
+--Sidebar--+--Main（中央・メインコンテンツ）--+--Right Sidebar--+
| ホーム    |  [Tabs]                          |  [About / etc.] |
| フォロー中 |  [Card list]                    |                 |
| 注目      |                                  |                 |
| 探索      |                                  |                 |
+-----------+----------------------------------+-----------------+
```

**Header**：
- 左：蓮の花アイコン（`img/works-logue-logo-icon.png`） + "Works Logue" テキスト
- 右：通知ベルボタン・ログインボタン・**+ Seed** ボタン

**Sidebar（左）**：
- ナビ項目：ホーム・フォロー中・注目・探索
- アクティブ項目はハイライト表示

**Right Sidebar（右）**：
- ホーム画面：About Works Logue カード
  - サービス説明テキスト
  - Stats（Seeds / Louges / Contributors）
  - 「Seedを投稿する」CTAボタン（`bg-primary-light`）
- Planter ページ（flowering）：貢献者一覧＋インサイトスコア

**ホームフィード（Tabs + Cards）**：

タブ：新着 / 人気 / 開花済み

カード構造（1枚）：
```
[Stage Badge] [投稿タイプ] · [アバター] [ユーザー名] · [時間]
[タイトル]
[Tag] [Tag] [Tag]
[X logs]  [X contributors]          [====Progress Bar====]
```

- **Stage Badge**：
  - `seeded` / growing → `Seed` or `Sprout`：`bg-primary-light`・`text-primary`
  - `flowering` → `Louge`：`bg-primary`・`text-white`
- **Progress Bar**（3px 高さ・120px 幅）：条件A 構造パーツ充足率を視覚化
  - Track: `bg-primary-light`
  - Fill: `bg-primary`（`louge` は全幅）、`sprout` 中は `bg-primary/50`

**Planter ページ（flowering 状態）のメインコンテンツ構造**：

```
[タイトル]
[元の Seed]（折りたたみ可能・展開で本文表示）
[Louge 記事本文]
[Log 一覧]
```

右パネル：貢献者一覧とインサイトスコア

### FR-06: Louge 開花（AI記事生成）
- FR-05 の **条件A（構造充足）AND 条件B（成熟度スコア基準突破）** を満たした時点で、バックグラウンドジョブが起動
- Vertex AI が Seed + Log 群を解析し、**パターンランゲージ形式**（状況→問題→解決策＋パターン名）を軸としたナレッジ記事（Louge）を生成
  - テーマ・議論の性質に応じて最適な構成を AI が判断（対立軸の整理・ステップバイステップ・チェックリスト・事例集 など）
  - 出典（元 Log）へのリンクを必ず含める
  - 反論/例外セクションを必ず含める（条件B で担保済み）
- 開花後も **同一の Planter ページ** で表示（別ページへの遷移なし）
  - ページ内で Louge 記事・Seed・Log が共存する
  - Planter の状態が `sprout` → `louge` に変化することで表示が切り替わる
- 開花に貢献した Log 投稿者のインサイトスコアを更新
- **Louge コピー機能**: Louge 記事（Markdown）をクリップボードにコピーするボタン。Seed・Log は含めず Louge 本文のみ

### FR-07: インサイトスコア
- 各ユーザーの Louge 開花への貢献度を数値化
- Log が Louge 生成に採用・参照された度合いに基づき加算
- プロフィールページに表示
- スコア計算は Louge 開花時に実行（非同期）

### FR-08: フォロー機能 ⚠️ 優先度：低
- **Planter フォロー**：任意の Planter をフォローできる
  - Seed を投稿したユーザーはその Planter を自動でフォロー
  - Log を投稿したユーザーも自動フォロー（検討余地あり）
- **ユーザーフォロー**：任意のユーザーをフォローできる
- フォロー中の Planter・ユーザーはサイドバー「フォロー中」フィードに表示

**通知**（フォロー機能と連動、優先度：低）：
- フォロー中 Planter に新しい Log が投稿された
- フォロー中 Planter の状態が変化した（sprout 昇格・louge 開花）
- フォロー中ユーザーが新しい Seed を投稿した

> **設計上の注意**：通知機能は初期 MVP では実装しないが、DB スキーマ（follows テーブル）は最初から用意しておき、後から通知を追加しやすい設計にする。

### FR-09: フィード・検索・タグ
- トップフィード（新着・注目・開花済み）
- タグ（業界・職種・スキル）によるフィルタリング
- キーワード検索（Supabase full-text search）

---

## 非機能要件

### NFR-01: テックスタック（確定）

| レイヤー | 技術 | 詳細 |
|---|---|---|
| Frontend | Next.js 14+ (App Router) + TypeScript strict | shadcn/ui + Tailwind CSS |
| Backend | Python FastAPI | Ruff + mypy strict |
| Database | Supabase (PostgreSQL + Auth + Storage) | 新規プロジェクト作成が必要 |
| AI | Vertex AI (Google Cloud) | 新規 GCP プロジェクト作成が必要 |
| Deploy | Cloud Run (web / api 独立) + CI/CD | 初期フェーズから設定 |

### NFR-02: デプロイ・インフラ
- Cloud Run に `apps/web`（Next.js）と `apps/api`（FastAPI）を独立デプロイ
- GitHub Actions による CI/CD パイプライン（lint → test → deploy）
- Supabase migrations で DB スキーマをバージョン管理

### NFR-03: ログ・モニタリング
- Cloud Run 向け構造化 JSON ログ（Cloud Logging）
- アプリケーションエラーは Sentry または Cloud Error Reporting

### NFR-04: パフォーマンス
- 初期フェーズではカバレッジ100%・最適化は不要
- Louge 生成・スコアリング（AI処理）は非同期バックグラウンドジョブで実行

### NFR-05: テスト方針
- Frontend: Vertex AI呼び出し周り・主要APIルート・核心フロー（Seed→Log→Louge）のE2E（Playwright）
- Backend: pytest でAPIエンドポイント + AIパイプライン（スコアエンジン含む）をカバー
- Security 拡張: **無効**（MVP フェーズ）
- Property-Based Testing 拡張: **無効**（MVP フェーズ）

### NFR-06: デザイン
- カラー: ティールグリーン（#00B4CC）+ クリーム背景 + ダークフォレストグリーン（#1A5C42）
- アイコン: Lucide（絵文字禁止）
- 雰囲気: ボタニカル・オーガニック（`docs/design-style.md` 準拠）

---

## DB スキーマ設計上の注意

MVP 実装時点から考慮しておくべき設計方針。

### 必須（後から変えるとコストが高いもの）

| 対象 | 方針 |
|---|---|
| `planters.parent_planter_id` | nullable FK。Fork 機能（フェーズ2）に備え最初から定義する |
| `logs.parent_log_id` | nullable FK。返信ネスト（ネスト1段）のために必要 |
| ソフトデリート | `planters` / `logs` / `users` に `deleted_at` カラムを持つ。物理削除はしない |
| `tags` 正規化 | タグはテキスト配列ではなく正規化テーブル（`tags` + 中間テーブル）で管理。探索・フィルタ・集計のため |
| `notifications` テーブル | 通知送信は後回しだが、イベント発生時（Log投稿・状態変化・開花）にレコードを積む設計にしておく |
| `planter_views` | 「注目」フィードのランキング根拠。後付けでは正確な集計が取れないため最初から計上する |

### 推奨（後で楽になるもの）

| 対象 | 方針 |
|---|---|
| `louge_score_snapshots` | Log 投稿のたびにスコアを記録するテーブル。閾値チューニング・分析に使う（スコアエンジン改良の前提） |
| `insight_score_events` | インサイトスコアの加算イベントを記録。スコア内訳の表示・バッジ判定（フェーズ2）の根拠になる |
| フォローテーブルの設計 | `planter_follows` と `user_follows` を分けるか polymorphic にするかを最初に決める（タグフォロー等の拡張を考慮） |

---

## フェーズ2以降

- バッジシステム（開花貢献者バッジ）
- Fork（Louge から派生 Seed）機能
- AIによる知恵洗浄（プライバシー保護・機密情報除去）
- 通知機能（FR-08 で設計済み・DB スキーマは MVP に含める）
- インサイト予測・レコメンド通知（「あなたの経験が役立つ」通知）
- BtoB タレントプール・マーケティングインサイト機能

---

## セットアップ前提条件

以下は開発開始前に手動で準備が必要：
1. Supabase プロジェクト作成（無料枠）→ URL・anon key・service_role key 取得
2. GCP プロジェクト作成 → Vertex AI API 有効化 → サービスアカウント作成
3. Google OAuth クライアント設定（Supabase Auth Providers）
